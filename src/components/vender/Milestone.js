import React from "react";

function Milestone() {
  return (
    <div className="comp">
      <input
        type="text"
        // value={item.name}
        // onChange={(e) => handlename(item.id, e)}
      />
      <p>Milestone</p>
      <input
        type="text"
        // value={item.name}
        // onChange={(e) => handlename(item.id, e)}
      />
      <p>%</p>
      <button className="mile-btn">x</button>
    </div>
  );
}
export default Milestone;
